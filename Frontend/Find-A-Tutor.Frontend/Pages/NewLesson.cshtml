@page
@model Find_A_Tutor.Frontend.Pages.NewLessonModel
@{
    ViewData["Title"] = "NewLesson";
}
<link rel="stylesheet" type="text/css" href="~/css/newLesson.css">

<div class="container">
    <div class="row">
        <div class="col-6">
            <h2>Utwórz nową korepetycję</h2>
        </div>
        <div class="col-6">
            <a href="/Student">
                <p class="text-right">Powrót do panelu studenta</p>
            </a>
        </div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-12">
            <form method="post">
                <p>Opis</p>
                <input type="text" name="description" placeholder="Opis" required="" />
                <p>Nazwa przedmiotu</p>
                @Html.DropDownList("schoolSubjectsList", new SelectList(Model.SchoolSubjects.Select(x => new { Value = x.Id, Text = x.Name }), "Value", "Text"))
                <p>Ogłoszenie ważne do</p>
                <input type="date" name="relevantTo" required="" />
                <button class="btn btn-lg btn-primary btn-block" type="submit">Wyślij</button>
            </form>
            @foreach(var msg in Model.Messages)
            {
                <h3 class="msg">@msg</h3>
            }
        </div>
    </div>

</div>


